$kind: http-request
name: 'Delete User'
method: DELETE
url: '{{base_url}}/users/:user_id'
order: 2500
headers:
  - key: Accept
    value: application/json
pathVariables:
  - key: user_id
    value: '{{user_id}}'
